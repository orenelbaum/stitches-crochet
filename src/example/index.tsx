import React from "react"
import { render } from "react-dom"
import { App } from "./app.component"
// import { applyGlobalStyles } from "./styling/apply-global-styles"

const root = document.getElementById("root")
render(<App />, root)

// applyGlobalStyles()
